# BeEnクイズアプリ — いままでの作業のまとめ

このメモは、**中学生の人でもだいたいわかる言葉**で書いています。専門用語が出たら、そのあとで短く説明します。

---

## このアプリは何？

**BeEngineer（BeEn）のクイズ**をブラウザで解くためのWebアプリです。

- **コース**（例：チャットアプリ開発コース）を選ぶ  
- **分野**（例：HTML・CSS）ごとに整理されている  
- **単元**（1つの授業のまとまり）を選んで問題を読み込む  

という流れで勉強できます。

データは **`catalog.json`（目次）** と、たくさんの **`unitXX.json`（各単元の問題集）** に入っています。

---

## 作業①：GitHub から手元のパソコンにコピーした

**Git（ギット）**は、プログラムの「バージョン管理」と「共有」のための道具です。

- ネット上の **GitHub** にあった同じアプリのデータ  
  （例：`https://github.com/keigo-fujiwara/BeEn_HW`）  
- を、あなたのパソコンのフォルダ **`BeEn_quiz_app`** に **クローン（まるごとコピー）** しました。

これで、自分のパソコンでファイルを開いたり、直したりできるようになりました。

---

## 作業②：「catalog.json が読めない」という表示の意味

ブラウザで **`index.html` をファイルとして直接開く**（アドレスが `file://...` になる）と、セキュリティの都合で **`fetch` という命令で別のJSONファイルを読めない**ことがあります。

だからアプリは、

> HTTPサーバーでフォルダを開いてからアクセスしてね（`file://` だと `fetch` が使えません）

のような説明を出します。

**やること（具体手順）**

1. **ターミナル**（黒い画面・PowerShell など）を開く  
2. プロジェクトのフォルダに移動する（例：`cd c:\Users\fuka1\BeEn_quiz_app`）  
3. 次のどちらか **1つ** を実行して、ローカルサーバーを起動する  

   **A. Python がある場合（おすすめ・追加インストール不要）**

   ```bash
   cd c:\Users\fuka1\BeEn_quiz_app
   python -m http.server 8080
   ```

   **B. Node.js がある場合**

   ```bash
   cd c:\Users\fuka1\BeEn_quiz_app
   npx --yes serve .
   ```

   画面に表示された番号（例：`http://localhost:3000`）をメモする。Python の例では **`8080`** 番。

4. **ブラウザ**（Chrome など）のアドレス欄に次を入力して Enter  

   - Python で `8080` を使った場合 → **`http://localhost:8080/index.html`**  
   - `serve` の場合 → ターミナルに出た URL の末尾に `index.html` を付けてもよい  

5. 終わったら、ターミナルで **`Ctrl + C`** を押してサーバーを止める  

こうすると `catalog.json` も `unitXX.json` も普通に読み込めます。

---

## 作業③：`catalog.json` には何が書いてある？

**目次のようなファイル**です。

- どんな **コース** があるか  
- その中にどんな **分野** があるか  
- 各単元の **名前** と、問題データの **場所（`jsonPath`）**  

が書いてあります。**問題の本文そのものはここには入っていません。**  
問題は `jsonPath` が指す `unitXX.json` の中にあります。

---

## 作業④：単元を「1つだけ」から「複数まとめて」選べるようにした

### 以前

- 単元は **1つだけ** 選べた  
- その1つの `unitXX.json` だけを読み込んでいた  

### いま

- 単元に **チェックボックス** がついた（複数にチェックできる）  
- **「選択した単元を読み込む」** ボタンを押すと、チェックした分の JSON を **まとめて読み込む**  
- 問題は **全部つなげた1つの大きな問題リスト** として出る  

**イメージ**：国語のドリル1冊＋算数のドリル1冊を、上から順に綴じて1冊にした感じ。

### 技術的に何をした？（ざっくり）

| やったこと | ひとことで |
|------------|------------|
| `index.html` を直した | ドロップダウンではなく、チェックボックスのエリアにした |
| `styles.css` を直した | 見た目（はみ出し防止・スクロールなど）を整えた |
| `app.js` を直した | 選ばれた単元を全部 `fetch` して、問題の配列をつなげた |

複数のファイルを同時に取りに行くときは **`Promise.all`** という仕組みを使っています。「みんなで同時に取りに行って、そろったら次へ」のイメージです。

問題の **ID がかぶらないように**、複数単元をつなげるときは  
`単元のID:元の問題のID` の形にしています。

---

## ファイルの役割（おさらい）

| ファイル・フォルダ | 役割 |
|-------------------|------|
| `index.html` | 画面の骨組み |
| `styles.css` | 色やレイアウトなどの見た目 |
| `app.js` | ボタンが押されたときの動き・データの読み込み |
| `data/catalog.json` | コース・分野・単元の「目次」 |
| `data/.../unitXX.json` | 各単元の問題データ |

---

## 自分で試すとき

1. このフォルダで **ローカルサーバー** を起動する（`file://` で開かない）  
2. ブラウザでアプリを開く  
3. コースを選ぶ → **複数の単元にチェック** → **選択した単元を読み込む**  
4. 宿題モードや確認テストで、つながった問題が出るか確かめる  

---

## 英単語ミニ辞典（読むときの助け）

- **Git / GitHub**：プログラムを保存・共有するためのサービスや道具  
- **クローン**：リモートのリポジトリを、手元にまるごとコピーすること  
- **fetch**：JavaScriptから「URLのデータを取ってきて」とお願いすること  
- **JSON**：データを `{ "名前": 値 }` の形で書いたファイル形式  
- **HTTPサーバー**：ブラウザが `http://...` でページやファイルを取りに行くための受け渡し役  

---

*このメモは、これまでの会話で行った作業内容を整理したものです。*
